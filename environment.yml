name: acds_debi

channels:
  - conda-forge
  - pytorch
  - defaults

dependencies:
 - flake8
 - jupyter
 - pip
 - pytest
 - pytest-timeout
 - python=3.10
 - sphinx
 - torch
 - torchvision
 - livelossplot
 - pillow
 - imageio
 - tqdm
 - pip:
   - -r requirements.txt
